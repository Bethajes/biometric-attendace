from django.contrib import admin

from attendance.models import BiometricDevice, DeviceCommand, DeviceEvent

# Admin registrations are now in attendance/admin.py
# This file is kept for backward compatibility with device_manager app.
